# Local dev needs nothing here — Vite proxies /api to http://127.0.0.1:8000 (see vite.config.js).
# For a DEPLOYED frontend, point this at your deployed backend and rebuild:
# VITE_API_BASE=https://your-backend.onrender.com
